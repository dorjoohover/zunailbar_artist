export interface IWarehouse {
  id?: string;
  name?: string;
  address?: string;
  created_at?: Date;
}
export interface Warehouse {
  id: string;
  name: string;
  address: string;
  created_at?: Date;
}
